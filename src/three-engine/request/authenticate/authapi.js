import bimhomeConfig from '../../config/bimhomeconfig';

const config = {
    headers: { 'Content-Type': 'application/json' },
};

const authapi = {

    // login(loginData) {
    //     return Axios.post(bimhomeConfig.AuthUrl, loginData, config)
    // }
}

export default authapi